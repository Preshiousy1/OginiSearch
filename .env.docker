# Database
MONGODB_URI=mongodb://mongodb:27017/ogini
ROCKSDB_PATH=/usr/src/app/data/rocksdb
# Term postings use a chunked schema (max 5000 doc IDs per document). For existing DBs, run:
# npx ts-node scripts/migrate-term-postings-to-chunked.ts

# Application
PORT=3000
NODE_ENV=development 